if statement=>

if statement is more powerful statement of decision making. It change change the flow of execution.

syntax:->

if(test-condition)
{
    statement;
}
statement;

test-condition=>either a expression or some static value.If test-consdition is a expression then the expression evaluates first and produce a boolean value.

if the value is true then inside the statement of if which we can say body of if executed other wise it will execute another line of code.

example:-

inside the if_else.js file we demonstrate the example.

Task-1:-check whether a number is even number.

if-else statement

syntax:-

if(test_condition)
{
    statement;
}
else
{
    statement;
}